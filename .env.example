# WHOOP Developer API credentials
# Register your app at https://developer-dashboard.whoop.com
WHOOP_CLIENT_ID=your_client_id_here
WHOOP_CLIENT_SECRET=your_client_secret_here

# Optional: Override token (skip OAuth flow entirely)
# WHOOP_ACCESS_TOKEN=your_access_token_here

# Optional: Override config directory (default: ~/.config/whoop-cli)
# WHOOP_CONFIG_DIR=/path/to/config
